---
import BaseLayout from "../../layouts/BaseLayout.astro";
import AdminAppShell from "@/components/AdminAppShell.astro";
import users from "@/data/users.json";
import type { User } from "@/features/admin/types";
import { getUserSlug } from "@/features/admin/utils";

export function getStaticPaths() {
  return (users as User[]).map((user) => ({
    params: { id: getUserSlug(user) },
  }));
}
---

<BaseLayout title="User · Local Guide Admin" bodyClass="bg-background" showHeader={false} showFooter={false}>
  <AdminAppShell slot="main" />
</BaseLayout>
