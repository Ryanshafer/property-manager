---
import LocalGuideAdminApp from "@/components/LocalGuideAdminApp";

interface Props {
  slot?: string;
}

const { slot } = Astro.props as Props;
const FALLBACK_ID = "admin-app-fallback";
---

<div slot={slot} class="min-h-dvh bg-background text-foreground">
  <div id={FALLBACK_ID} class="flex min-h-dvh items-center justify-center gap-2 text-muted-foreground">
    <svg class="h-5 w-5 animate-spin" viewBox="0 0 24 24" aria-hidden="true">
      <path
        d="M21 12a9 9 0 1 1-6.219-8.56"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
      />
    </svg>
    <span>Loading…</span>
  </div>
  <LocalGuideAdminApp client:only="react" />
</div>
